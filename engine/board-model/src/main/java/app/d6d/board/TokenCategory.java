package app.d6d.board;

/** Natura narrativa di una pedina di scena; l'immagine è un asse separato. */
public enum TokenCategory {
    CHARACTER,
    ALLY,
    NPC,
    MONSTER,
    OBJECT,
    TRAP,
    HAZARD,
    TERRAIN,
    LOOT,
    VEHICLE,
    MARKER,
    OTHER
}
